import{useState,useEffect}from'react';import{NavLink,useLocation,useNavigate}from'react-router-dom';import{LayoutDashboard,Waypoints,MonitorUp,Server,History,ScrollText,Settings,Bell,Search,Menu,ChevronLeft,LogOut,Sun,Moon,ShieldCheck,X,Radio,UsersRound}from'lucide-react';import{authService}from'../services/mockServices';import{notifications}from'../mocks/data';import{Status}from'./UI';
const nav=[['/dashboard','Overview',LayoutDashboard],['/tunnel','Tunnel Monitoring',Waypoints],['/remote','Remote Access',MonitorUp],['/server','Server Health',Server],['/connections','Connection History',History],['/logs','Activity Logs',ScrollText],['/settings','Settings',Settings]] as const;
export default function Layout({children}:{children:React.ReactNode}){const[collapsed,setCollapsed]=useState(false),[mobile,setMobile]=useState(false),[notice,setNotice]=useState(false),[theme,setTheme]=useState(localStorage.getItem('wx_theme')||'dark');const loc=useLocation(),go=useNavigate();useEffect(()=>{document.documentElement.dataset.theme=theme;localStorage.setItem('wx_theme',theme)},[theme]);const title=nav.find(n=>n[0]===loc.pathname)?.[1]||'Overview';return <div className={`app ${collapsed?'collapsed':''}`}><aside className={mobile?'open':''}><div className="brand"><Logo/><div><strong>WEBXORA</strong><small>TUNNELING</small></div><button className="icon-btn mobile-close" onClick={()=>setMobile(false)}><X/></button></div><div className="tagline">Secure Infrastructure Control</div><nav>{nav.map(([to,label,I])=><NavLink key={to} to={to} end={to==='/dashboard'} onClick={()=>setMobile(false)} title={label}><I/><span>{label}</span></NavLink>)}</nav><div className="health"><div><Radio/><span>System health</span></div><strong>99.98%</strong><Status label="Operational"/></div><div className="side-profile"><div className="avatar">JR</div><div><b>John Regala</b><small>Authorized Administrator</small></div><button className="icon-btn" title="Logout" onClick={()=>{authService.logout();go('/login')}}><LogOut/></button></div><button className="collapse icon-btn" onClick={()=>setCollapsed(!collapsed)}><ChevronLeft/></button></aside>{mobile&&<div className="drawer-shade" onClick={()=>setMobile(false)}/>}<main><header><div className="header-left"><button className="icon-btn menu" onClick={()=>setMobile(true)}><Menu/></button><div className="crumb"><small>WEBXORA / MONITORING</small><b>{title}</b></div></div><label className="global-search"><Search/><input placeholder="Search infrastructure…"/></label><div className="header-tools"><Status label="Live"/><time>{new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time><button className="icon-btn" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?<Sun/>:<Moon/>}</button><button className="icon-btn notification" onClick={()=>setNotice(true)}><Bell/><i/></button><div className="avatar">JR</div></div></header><div className="page"><div className="pagehead"><div><div className="breadcrumbs">Console / {title}</div><h1>{title}</h1><p>{title==='Overview'?'Real-time visibility across your secure infrastructure.':'Monitor and manage your WebXora infrastructure.'}</p></div></div>{children}</div></main>{notice&&<div className="notice-shade" onClick={()=>setNotice(false)}><div className="notice-drawer" onClick={e=>e.stopPropagation()}><div className="notice-head"><div><h2>Notifications</h2><p>System alerts and activity</p></div><button className="icon-btn" onClick={()=>setNotice(false)}><X/></button></div>{notifications.map((n,i)=><article className={`notice ${n.severity}`} key={i}><Bell/><div><b>{n.title}</b><p>{n.text}</p><small>{n.time}</small></div></article>)}</div></div>}</div>}
function Logo(){return <div className="logo"><ShieldCheck/></div>}
