import{ReactNode,useEffect}from'react';import{CheckCircle2,AlertTriangle,XCircle,Info,LoaderCircle,X}from'lucide-react';
export function Status({label}: {label:string}){const l=label.toLowerCase();const tone=/online|connected|running|healthy|available|ready|active|success|secure|operational/.test(l)?'success':/warning|connecting|degraded/.test(l)?'warning':/offline|failed|error|disconnected/.test(l)?'danger':'muted';return <span className={`status ${tone}`}><i/>{label}</span>}
export function Card({children,className=''}:{children:ReactNode,className?:string}){return <section className={`card ${className}`}>{children}</section>}
export function Button({children,variant='primary',loading=false,...p}:React.ButtonHTMLAttributes<HTMLButtonElement>&{variant?:string;loading?:boolean}){return <button className={`btn ${variant}`} disabled={loading||p.disabled}{...p}>{loading&&<LoaderCircle size={16} className="spin"/>}{children}</button>}
export function Modal({title,children,onClose}:{title:string;children:ReactNode;onClose:()=>void}){useEffect(()=>{const f=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();document.addEventListener('keydown',f);return()=>document.removeEventListener('keydown',f)},[onClose]);return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="modal" role="dialog" aria-modal="true" aria-label={title}><button className="icon-btn modal-x" onClick={onClose}><X size={18}/></button><h2>{title}</h2>{children}</div></div>}
export function Empty(){return <div className="empty"><Info/><b>No results found</b><span>Try adjusting your search or filters.</span></div>}
export const resultIcon=(r:string)=>r==='Success'?<CheckCircle2/>:r==='Warning'?<AlertTriangle/>:r==='Failed'?<XCircle/>:<Info/>;
