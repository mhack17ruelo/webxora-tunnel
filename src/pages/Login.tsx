import{FormEvent,useEffect,useState}from'react';import{useNavigate}from'react-router-dom';import{ShieldCheck,Eye,EyeOff,LockKeyhole,Waypoints,Activity,CheckCircle2}from'lucide-react';import{authService,logService}from'../services/mockServices';import{Button}from'../components/UI';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[remember,setRemember]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState(''),[loading,setLoading]=useState(false),go=useNavigate();useEffect(()=>{document.title='Sign In | WEBXORA TUNNELING'},[]);async function submit(e:FormEvent){e.preventDefault();setError('');setMessage('');if(!email.trim()||!password){setError('Enter your email and password.');return}setLoading(true);setMessage('Authenticating your WEBXORA account...');try{await authService.login(email.trim(),password,remember);logService.add('Successful login','Authentication','John Regala authenticated successfully','Success','Current browser','203.177.68.125');setMessage('Login successful. Welcome, John Regala.');await new Promise(r=>setTimeout(r,700));go('/register-computer')}catch(e){setError((e as Error).message);setMessage('');logService.add('Failed login attempt','Security','Unable to authenticate supplied credentials','Failed','Unknown browser','203.177.68.125')}finally{setLoading(false)}}return <div className="login"><section className="login-visual"><div className="login-brand"><div className="logo"><ShieldCheck/></div><div><b>WEBXORA TUNNELING</b><span>Secure Tunnel and Remote Access Monitoring</span></div></div><div className="network-visual"><div className="orb center"><ShieldCheck/></div><div className="orb o1"><Waypoints/></div><div className="orb o2"><Activity/></div><div className="orb o3"><LockKeyhole/></div></div><div className="visual-copy"><small>SECURE INFRASTRUCTURE CONTROL</small><h1>Your network.<br/><em>Under control.</em></h1><p>Register, secure, and monitor your computer through one trusted operations console.</p><div className="feature-row"><span><CheckCircle2/>End-to-end monitoring</span><span><CheckCircle2/>Audit-ready events</span></div></div><div className="availability"><span>● ALL SYSTEMS OPERATIONAL</span><b>99.98% availability</b></div></section><section className="login-form-wrap"><form className="login-card" onSubmit={submit}><div className="login-mobile-brand"><div className="logo"><ShieldCheck/></div><b>WEBXORA</b></div><div className="secure-label"><LockKeyhole/>SECURE ADMIN PORTAL</div><h2>Welcome back</h2><p>Sign in to register and monitor your WebXora computer.</p><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="john.regala@gmail.com" autoComplete="username"/></label><label>Password<div className="password"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" autoComplete="current-password"/><button type="button" onClick={()=>setShow(!show)} aria-label="Toggle password visibility">{show?<EyeOff/>:<Eye/>}</button></div></label><div className="form-options"><label className="check"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/>Remember me</label><button type="button" className="link" onClick={()=>setError('Password recovery is unavailable in this mock environment.')}>Forgot password?</button></div>{message&&<div className="form-message" role="status">{message}</div>}{error&&<div className="form-error" role="alert">{error}</div>}<Button loading={loading} type="submit">{loading?'Authenticating…':'Sign In'}</Button><small className="session"><ShieldCheck/>Protected mock session · Passwords are never stored</small></form></section></div>}
